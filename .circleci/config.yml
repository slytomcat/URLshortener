version: 2.1
jobs:

  test:
    docker:
      - image: circleci/golang:1.14
    steps:
      - checkout
      - run:
          name: Prepare
          command: |
            go get -u github.com/jstemmer/go-junit-report;
            mkdir results;
      - run:
          name: Tests
          command: go test -v -race -timeout 60s -coverprofile cover.out 2>&1 | tee /dev/tty | go-junit-report > results/results.xml
      - store_test_results:
          path: results
      - persist_to_workspace:
          root: .
          paths:
            - ./cover.out

  report:
    docker:
      - image: circleci/golang:1.14
    steps:
      - checkout
      - attach_workspace:
          at: ./artifacts
      - run:
          name: Join coverage files
          command: cat ./artifacts/cover* > ./cover.out
      - run:
          name: Report coverage to deepsource.io
          command: |
            curl https://deepsource.io/cli | sh
            ./bin/deepsource report --analyzer test-coverage --key go --value-file ./cover.out
      - run:
          name: Format coverage
          command: go tool cover -html=cover.out -o coverage.html
      - store_artifacts:
          path: coverage.html

  build:
    docker:
      - image: circleci/golang:1.14
    steps:
      - checkout
      - run:
          name: building
          command: CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -a -installsuffix cgo .
      - run:
          name: Store version
          command: git describe --tags | sed "s/^\(v\.[0-9]\+\.[0-9]\+\.[0-9]\+\).*/\1/" > VERSION
      - persist_to_workspace:
          root: .
          paths:
            - ./URLshortener
            - ./VERSION

  upload_github:
    docker:
      - image: 'cibuilds/github:0.10'
    steps:
      - attach_workspace:
          at: ./artifacts
      - run:
          name: Publish Release on GitHub
          command: |
            TAG=$(cat ./artifacts/VERSION)
            echo "TAG: $TAG"
            cp ./artifacts/URLshortener ./URLshortener-amd64
            ghr -t ${GHAPITOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -c ${CIRCLE_SHA1} -delete ${TAG} ./URLshortener-amd64

  upload_docker:
    docker:
      - image: circleci/golang:1.14
    steps:
      - checkout
      - attach_workspace:
          at: ./artifacts
      - setup_remote_docker
      - run:
          name: Build and push Release to Docker.io
          command: |
            TAG=$(cat ./artifacts/VERSION)
            echo "TAG: $TAG"
            cp ./artifacts/URLshortener .
            echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin
            docker image build -t slytomcat/urlshortener:latest .
            docker tag slytomcat/urlshortener:latest slytomcat/urlshortener:$TAG
            docker push slytomcat/urlshortener

workflows:
  version: 2.1
  build_test_upload:
    jobs:
      - test
      - report:
          requires:
            - test
      - build
      - upload_github:
          requires:
            - build
          filters:
            branches:
              only: master
      - upload_docker:
          requires:
            - build
          filters:
            branches:
              only: master
